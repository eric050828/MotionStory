# Prefer binary wheels to avoid Rust compilation on Render.com
--prefer-binary

# FastAPI & ASGI Server
fastapi==0.110.0
uvicorn[standard]==0.27.0

# Database
motor==3.3.2  # MongoDB async driver
pymongo==4.6.0

# Data Validation
pydantic==2.8.0  # Python 3.13 support with binary wheels
pydantic-settings==2.8.0

# Authentication
firebase-admin==6.4.0
PyJWT==2.8.0
passlib[argon2]==1.7.4
cryptography<35.0.0  # Pin to last C-only version (avoid Rust)

# Cloud Storage (Cloudflare R2)
boto3==1.34.0

# Utilities
python-multipart==0.0.9  # File uploads
python-dotenv==1.0.0

# Testing
pytest==8.0.0
pytest-asyncio==0.23.0
pytest-cov==4.1.0
httpx==0.26.0

# Code Quality
black==24.1.0
isort==5.13.0
flake8==7.0.0

# Scheduler (for cleanup tasks)
APScheduler==3.10.4
